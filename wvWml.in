#!/bin/sh

prefix=@prefix@
exec_prefix=@exec_prefix@
datadir=@datadir@

# argument checking
if [ ${#} -ne "2" ]; then
	echo "Usage: ${0} <word document> <wml output file>"
	exit 1
fi

wvWare -x ${datadir}/wv/wvWml.xml "${1}" > "${2}"
