CC = @CC@

LEX = @LEX@

SHELL=/bin/sh

@SET_MAKE@

srcdir=@srcdir@

CFLAGS = @CFLAGS@ @DEFS@ -I$(srcdir)/ -I$(srcdir)/..  @WMF_CFLAGS@ -DTTFDIR=\"$(libdir)/mswordview/ttf\"

SHELL = /bin/sh

.SUFFIXES: .C .o

MYOBJECTS =  oledecod.o

all: oledecod.o

%.o: $(srcdir)/%.c 
	$(CC) $(CFLAGS) -c $(srcdir)/$*.c 

clean:	
	rm -rf $(MYOBJECTS) core 

realclean:  
	rm -rf config.cache config.status config.log Makefile *.o core demo

#  DO  NOT  DELETE THIS LINE -- make depend depends on it.
